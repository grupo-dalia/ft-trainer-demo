/* Explorador de rutinas: carpetas y plantillas reutilizables. */
const routinePageBase=pages.routines;
pages.routines=()=>`${routinePageBase()}<section class="card routine-explorer" id="routine-explorer"><div class="section-head"><div><p class="eyebrow">BIBLIOTECA</p><h2>Carpetas de rutinas</h2><p class="muted">Organiza plantillas por niveles, semanas o bloques.</p></div><button class="primary" id="new-folder">＋ Nueva carpeta</button></div><div class="routine-location" id="routine-location"><button type="button" class="back-button" id="routine-back" aria-label="Volver a la carpeta anterior">←</button><nav class="breadcrumbs" id="routine-breadcrumbs">Rutinas</nav></div><div class="folder-grid" id="folder-grid"><p class="muted">Cargando carpetas…</p></div></section>`;
const folderState={parent:null,folders:[],routines:[]};
async function loadRoutineFolders(){const grid=document.getElementById('folder-grid');if(!grid||!window.ftSupabase)return;const routineQuery=ftSupabase.from('routines').select('id,name,folder_id,description');const [folders,routines]=await Promise.all([ftSupabase.from('routine_folders').select('id,name,parent_id').order('name'),folderState.parent?routineQuery.eq('folder_id',folderState.parent):routineQuery.is('folder_id',null)]);if(folders.error||routines.error){grid.innerHTML='<p class="muted">No se pudieron cargar las carpetas.</p>';return}folderState.folders=folders.data||[];folderState.routines=routines.data||[];const current=folderState.folders.filter(f=>f.parent_id===folderState.parent),options=folderState.folders.map(f=>`<option value="${f.id}">${escapeHtml(f.name)}</option>`).join('');grid.innerHTML=[...current.map(f=>`<article class="folder-card" data-folder="${f.id}" tabindex="0" role="button"><div class="file-icon folder-icon">▰</div><b>${escapeHtml(f.name)}</b><div class="card-actions"><button type="button" class="rename-folder" title="Renombrar carpeta" aria-label="Renombrar ${escapeHtml(f.name)}">•••</button></div></article>`),...folderState.routines.map(r=>`<article class="routine-file"><div class="file-icon routine-icon">▤</div><div class="routine-copy"><b>${escapeHtml(r.name)}</b><small>${escapeHtml(r.description||'Plantilla')}</small></div><label class="move-control">Mover a<select class="folder-move" data-routine="${r.id}" aria-label="Mover rutina"><option value="">Rutinas (raíz)</option>${options}</select></label></article>`)].join('')||'<p class="muted">Esta carpeta está vacía.</p>';grid.querySelectorAll('.folder-card').forEach(card=>{const open=()=>{folderState.parent=card.dataset.folder;loadRoutineFolders()};card.onclick=e=>{if(!e.target.closest('button'))open()};card.onkeydown=e=>{if((e.key==='Enter'||e.key===' ')&&!e.target.closest('button')){e.preventDefault();open()}}});grid.querySelectorAll('.rename-folder').forEach(button=>button.onclick=async e=>{e.stopPropagation();const card=button.closest('[data-folder]'),name=prompt('Nuevo nombre de carpeta');if(name?.trim())await ftSupabase.from('routine_folders').update({name:name.trim(),updated_at:new Date().toISOString()}).eq('id',card.dataset.folder);loadRoutineFolders()});grid.querySelectorAll('.folder-move').forEach(select=>{select.value=folderState.parent||'';select.onchange=async()=>{await ftSupabase.from('routines').update({folder_id:select.value||null}).eq('id',select.dataset.routine);loadRoutineFolders()}});const currentName=folderState.parent?folderState.folders.find(f=>f.id===folderState.parent)?.name:'';document.getElementById('routine-breadcrumbs').textContent=folderState.parent?`Rutinas / ${currentName}`:'Rutinas';document.getElementById('routine-back').hidden=!folderState.parent}
document.addEventListener('click',e=>{if(e.target.id==='new-folder'){const name=prompt('Nombre de la carpeta');if(name?.trim())ftSupabase.from('routine_folders').insert({name:name.trim(),parent_id:folderState.parent}).then(loadRoutineFolders)}if(e.target.id==='routine-back'&&folderState.parent){const current=folderState.folders.find(f=>f.id===folderState.parent);folderState.parent=current?.parent_id||null;loadRoutineFolders()}});
const renderWithFolders=render;render=page=>{if(page!=='routines')folderState.parent=null;renderWithFolders(page);if(page==='routines')setTimeout(loadRoutineFolders,0)};
